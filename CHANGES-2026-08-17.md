# Requested showroom updates — 2026-08-17

Implemented:

- Appointment email body uses `ATELIER` in uppercase.
- Appointment WhatsApp/email `{time}` renders as `From h:mm AM/PM to h:mm AM/PM` and default templates label it `Time slot`.
- Appointment time selectors use 12-hour AM/PM labels in 15-minute increments.
- Appointment times are displayed as full From/To ranges in tables, overview rows, visits, calendar and PDF appointment reports.
- Blacklisted clients can be edited and deleted, with API support.
- Booking/edit validation restricts phones to digits, validates email format, and rejects duplicate phone/email values.
- Appointment list has a service filter for receptionist and manager views.
- Manager overview status donut is dynamic and contains only Walk-in, Arrived, Canceled and No-show.
- Appointments include a collapsed `Open calendar` control with Daily, Weekly, Monthly and Yearly views plus previous/today/next navigation.
- Weekly/Daily calendar uses a 15-minute schedule grid inspired by the supplied reference.

Validation performed:

- `npm run typecheck` — passed.
- `npm run lint` — 0 errors; two pre-existing Next.js `<img>` optimization warnings remain in image attachment previews.
- `npm run build` could not complete in the sandbox because Next.js attempted to download a Linux SWC package from `registry.npmjs.org`, while sandbox internet access is unavailable.
